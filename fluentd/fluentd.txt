#website: https://www.fluentd.org/


# docker image: https://hub.docker.com/_/fluentd
$> sudo docker run -p 24224:24224 -p 24224:24224/udp -u fluent -v /var/lib/docker/containers:/fluentd/log fluentd

# using custom configuration:
$> sudo docker run -ti --rm -v /var/lib/docker/containers:/fluentd/etc fluentd -c /fluentd/etc/<conf-file>



#example config.yaml
#see https://docs.fluentd.org/configuration/config-file-yaml
#see https://github.com/fluent/fluent-bit/blob/master/packaging/testing/smoke/container/fluent-bit.yaml
config:
  # Receive events from 24224/tcp
  # This is used by log forwarding and the fluent-cat command
  - source:
      $type: forward
      port: 24224

  # http://<ip>:9880/myapp.access?json={"event":"data"}
  - source:
      $type: http
      port: 9880


#using mongodb
#make your own docker file: https://github.com/jsliang/fluent-docker-image-plugin-mongo


